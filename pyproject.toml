[project]
name = "asimov-manus"
version = "0.1.0"
description = "Base estructural para un sistema multiagente jerárquico ASIMOV."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
